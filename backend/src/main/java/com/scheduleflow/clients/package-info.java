/**
 * Future OpenFeign client stubs.
 *
 * <p>This package will contain Declarative REST Clients (OpenFeign) in future microservice phases:
 * <ul>
 *   <li><strong>ResourceServiceClient</strong> — Fetches rooms and resource availability from Resource Service (Phase 4 & 6)</li>
 *   <li><strong>EventServiceClient</strong> — Sends events to Event Service (Phase 5)</li>
 *   <li><strong>NotificationClient</strong> — Dispatches notification requests (Phase 5)</li>
 * </ul>
 *
 * <p>To enable Feign clients in a future phase, add {@code @EnableFeignClients} to the main application class.
 */
package com.scheduleflow.clients;
